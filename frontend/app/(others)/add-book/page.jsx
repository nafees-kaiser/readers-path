import React from 'react';
import BookForm from "@/components/BookForm";

const Page = () => {
    return (
        <BookForm
            heading={"Add Book"}
        />
    );
};

export default Page;